# quickshell config

To get this config working, run this command in this directory:

```sh
ln -s $(pwd) ~/.config/quickshell
```

Also symlink the matugen config so theming works:

```sh
ln -s $(pwd)/matugen ~/.config/matugen
```
