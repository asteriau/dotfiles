# Sourced from /etc/xdg/hypr/hyprland.conf via programs.hyprland.settings.source
# (see system/programs/hyprland/settings.nix). Matugen mode populates this with
# overrides; preset mode truncates so the nix-defined static colors win.
general:col.active_border = rgba({{colors.outline_variant.default.hex_stripped}}77)
general:col.inactive_border = rgba({{colors.surface_container_low.default.hex_stripped}}33)
misc:background_color = rgba({{colors.surface.default.hex_stripped}}FF)
